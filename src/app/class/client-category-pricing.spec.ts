import { ClientCategoryPricing } from './client-category-pricing';

describe('ClientCategoryPricing', () => {
  it('should create an instance', () => {
    expect(new ClientCategoryPricing()).toBeTruthy();
  });
});
